DSEG SEGMENT
COUNT DW ?
SUM DW ?
DSEG ENDS

CSEG SEGMENT
ASSUME CS:CSEG,DS:DSEG
START:
MOV AX,DSEG
MOV DS,AX
MOV AX,0
MOV BX,0
BEGIN:
CMP AX,1000D
JA EXIT
INC BX
ADD AX,BX
JMP BEGIN

EXIT:
MOV COUNT,BX
MOV SUM,AX
MOV AH,4CH
INT 21H
CSEG ENDS
END START
