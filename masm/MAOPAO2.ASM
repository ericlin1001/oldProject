DSEG SEGMENT
BLOCK DB 12H,37H,25H,9H,43H,59H,17H,32H,67H,92H,85H,12H,5H,78H,21H
N DW $-BLOCK
DSEG ENDS

CSEG SEGMENT
ASSUME CS:CSEG,DS:DSEG
START:
MOV AX,DSEG
MOV DS,AX
MOV BX,OFFSET BLOCK
MOV CX,N
DEC CX
AGAIN1:
MOV DI,CX
MOV SI,0
AGAIN2:
MOV AL,[BX+SI]
CMP AL,[BX+SI+1]
JNB NEXT
XCHG AL,[BX+SI+1]
MOV [BX+SI],AL
NEXT:
INC SI
LOOP AGAIN2
MOV CX,DI
LOOP AGAIN1


EXIT:
MOV AH,4CH
INT 21H
CSEG ENDS
END START
